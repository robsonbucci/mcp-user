startCommand:
  type: http
  configSchema:
    type: object
    required: ["mongodbUri"]
    properties:
      mongodbUri:
        type: string
        title: "MongoDB URI"
        description: "URI de conexão do MongoDB (incluindo usuário e senha )"
